# ============================================================
#  homepage-nas/.gitignore
#  這個資料夾是獨立的 git repo（可直接 clone 到 NAS 部署）
# ============================================================

# ---- 機密：所有金鑰與帳密都集中在 .env，絕不進版控 ----
#      要改欄位請同步更新 .env.example（那份沒有值，只有說明）
.env
*.env
!.env.example

# ---- Homepage 執行時產生的東西 ----
config/logs/
images/

# Homepage 啟動時會把沒用到的整合補成空白預設檔（刪了還是會再生）。
# 哪天真的要接 Proxmox / K8s，把對應那行拿掉再 git add 就好。
config/kubernetes.yaml
config/proxmox.yaml

# ---- 編輯器 / 系統雜物 ----
*.bak
*.old
.DS_Store
Thumbs.db
